<div class="player-list">
  <p>プレイヤー設定</p>
  <% if @players.empty? %>
    <p>プレイヤーを登録しよう！</p>
  <% else %>
    <%= @players.each do |player| %>
      <%= player.name %>
      <p></p>
    <% end %>
  <% end %>
</div>