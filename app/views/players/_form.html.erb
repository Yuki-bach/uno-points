<div class="create-players">
  <%= form_with(model: @player, method: :post) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit "追加する", class: "btn btn-primary" %>
  <% end %>
</div>