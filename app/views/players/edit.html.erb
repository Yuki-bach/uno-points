<div class="how-to-count">
  <h1>点数の数え方</h1>
  <p>0-9の数字カードはその数字の点数</p>
  <p>スキップ、リバース、ドロー2は20点</p>
  <p>ワイルド、ワイルドドロー4は50点</p>
</div>

<div class="player-list">
  <ol>
    <%= @player.name %>
    <a>:   </a>
    <%= @player.points %>
    <%= form_with(model: @player) do |f| %>
      <%= f.label :points %>
      <%= f.text_field :points, value: nil %>
      <%= f.submit "変更", class: "btn btn-primary" %>

            <p></p>
    <% end %>
  </ol>
</div>